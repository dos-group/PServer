package de.tuberlin.pserver.types.collection;


import de.tuberlin.pserver.types.AbstractDistributedType;
import de.tuberlin.pserver.types.matrix.partitioner.MatrixPartitionShape;
import de.tuberlin.pserver.types.matrix.partitioner.MatrixPartitioner;
import de.tuberlin.pserver.types.matrix.partitioner.PartitionType;

public abstract class AbstractDistributedCollectionType extends AbstractDistributedType implements DistributedCollectionType {

    // ---------------------------------------------------
    // Fields.
    // ---------------------------------------------------

    // TODO!

    // ---------------------------------------------------
    // Constructor.
    // ---------------------------------------------------

    public AbstractDistributedCollectionType(int nodeID, int[] nodes) {
        super(nodeID, nodes);

        // TODO!
    }

    // ---------------------------------------------------
    // Public Methods.
    // ---------------------------------------------------

    // TODO!
}
