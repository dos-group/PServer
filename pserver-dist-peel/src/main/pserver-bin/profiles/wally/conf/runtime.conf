env.node.heap.mb = 12288
# be sure to only pass arguments via -o because this config is also used for scp!
env.ssh.opts = -o User=peel -o ForwardAgent=yes -o StrictHostKeyChecking=no
env.java.home = /usr/lib/jvm/java-1.8.0-openjdk/jre

# env.pid.dir = /tmp
# env.log.max = 5
# env.java.opts =
